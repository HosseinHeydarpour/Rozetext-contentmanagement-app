# 📱 راهنمای جامع راه‌اندازی سیستم مدیریت محتوای اینستاگرام
## Instagram Content Manager - Complete Setup & Installation Guide

این پروژه شامل دو بخش اصلی است:
1. **سرور کامپیوتر (`server/`)**: سرور محلی Node.js، داشبورد وب فارسی با تقویم شمسی، پوشه پایش خودکار (`watch_folder`)، استریم روان ویدیو و تولید کد QR برای اتصال مستقیم گوشی.
2. **اپلیکیشن اندروید فلاتر (`mobile_app/`)**: تقویم محتوای شمسی (امروز/فردا/آینده)، نمای شبکه ۳×۳ اینستاگرام، پخش‌کننده ویدیو، دانلود ۱-کلیکه ویدیو در گالری و کپی خودکار کپشن، و مدیریت وضعیت «منتشر شده».

---

## 🚀 بخش اول: اجرای سرور روی کامپیوتر (ساده‌ترین مرحله)

برای اجرای سرور کامپیوتر نیازی به دستورات پیچیده ندارید:

1. وارد پوشه `instagram-content-manager` شوید.
2. روی فایل **`start_server.bat`** دابل کلیک کنید.
3. در بار اول، پکیج‌های مورد نیاز به صورت خودکار نصب می‌شوند.
4. سرور اجرا شده و در محیط خط فرمان (CMD) آدرس داشبورد کامپیوتر، آدرس شبکه محلی (IP) و **کد QR اختصاصی** نمایش داده می‌شود:
   - **داشبورد کامپیوتر در مرورگر**: `http://localhost:3000`
   - **آدرس شبکه گوشی**: `http://192.168.x.x:3000`
5. مرورگر خود را باز کنید و وارد `http://localhost:3000` شوید تا پست‌های جدید با ویدیو، کاور، کپشن و تاریخ شمسی ثبت کنید.

### 📂 قابلیت پوشه پایش خودکار (Watch Folder):
داخل پوشه `server/watch_folder`، هر زمان یک فایل ویدیو (مانند `reel_01.mp4`) کپی کنید، سرور به صورت خودکار آن را شناسایی کرده و به عنوان یک پست پیش‌نویس ثبت می‌کند! اگر فایل متنی هم‌نام مانند `reel_01.txt` یا کاور `reel_01.jpg` کنار آن باشد، کپشن و کاور نیز خودکار خوانده می‌شوند.

---

## 🛠️ بخش دوم: نصب فلاتر (Flutter) در ویندوز

با توجه به اینکه فلاتر هنوز روی سیستم شما تنظیم نشده است، این ۴ مرحله ساده را دنبال کنید:

### مرحله ۱: دانلود فلاتر SDK
1. به صفحه رسمی دانلود فلاتر برای ویندوز بروید:
   👉 [https://docs.flutter.dev/get-started/install/windows/mobile](https://docs.flutter.dev/get-started/install/windows/mobile)
2. فایل فشرده فلاتر (مثلاً `flutter_windows_3.x.x-stable.zip`) را دانلود کنید.
3. فایل را در یک مسیر بدون فاصله و دسترسی آسان مانند **`C:\src\flutter`** استخراج (Extract) کنید.
   *(نکته: هرگز فلاتر را در `C:\Program Files` قرار ندهید)*

### مرحله ۲: افزودن فلاتر به متغیرهای سیستمی (PATH)
1. در منوی استارت ویندوز عبارت **Environment Variables** یا **ویرایش متغیرهای محیطی سیستم** را جستجو کنید و باز کنید.
2. روی دکمه **Environment Variables...** کلیک کنید.
3. در قسمت بالا (User variables)، متغیر **`Path`** را انتخاب کرده و دکمه **Edit** را بزنید.
4. دکمه **New** را بزنید و مسیر زیر را وارد کنید:
   ```
   C:\src\flutter\bin
   ```
5. همه پنجره‌ها را با **OK** ببندید.
6. یک ترمینال جدید (PowerShell یا CMD) باز کنید و دستور زیر را تایپ کنید:
   ```bash
   flutter --version
   ```
   اگر نسخه فلاتر نمایش داده شد، نصب با موفقیت انجام شده است!

### مرحله ۳: نصب اندروید استودیو (Android Studio)
1. نرم‌افزار [Android Studio](https://developer.android.com/studio) را دانلود و نصب کنید.
2. در حین نصب، اطمینان حاصل کنید **Android SDK Platform** و **Android Virtual Device** تیک خورده باشند.
3. پس از نصب، در تب **SDK Manager** > زیرمجموعه **SDK Tools**، تیک گزینه **Android SDK Command-line Tools (latest)** را بزنید و دکمه Apply را بزنید تا نصب شود.
4. در ترمینال دستور زیر را اجرا کنید تا مجوزهای اندروید تأیید شوند:
   ```bash
   flutter doctor --android-licenses
   ```
   (به تمام سوالات با تایپ `y` پاسخ دهید).

---

## 📲 بخش سوم: اجرای اپلیکیشن روی گوشی اندروید یا ساخت فایل نصب (APK)

### روش ۱: اجرای مستقیم روی گوشی از طریق کابل USB
1. در گوشی اندروید خود، وارد تنظیمات (Settings) > درباره گوشی (About Phone) شوید و ۷ بار روی **Build Number (شماره ساخت)** ضربه بزنید تا **Developer Options (گزینه‌های توسعه‌دهنده)** فعال شود.
2. وارد Developer Options شوید و تیک **USB Debugging** را فعال کنید.
3. گوشی را با کابل به کامپیوتر وصل کنید و در پیام نمایش داده شده روی گوشی، دسترسی را تأیید کنید.
4. در ترمینال وارد پوشه اپلیکیشن شوید:
   ```bash
   cd c:\Users\User\Desktop\instagram-content-manager\mobile_app
   flutter pub get
   flutter run
   ```
   اپلیکیشن روی گوشی شما نصب و اجرا می‌شود!

### روش ۲: ساخت مستقیم فایل نصبی APK برای انتقال به گوشی
اگر نمی‌خواهید از کابل استفاده کنید، می‌توانید مستقیماً فایل نصبی خروجی بگیرید:
```bash
cd c:\Users\User\Desktop\instagram-content-manager\mobile_app
flutter pub get
flutter build apk --release
```
فایل نصبی آماده در مسیر زیر ایجاد می‌شود و می‌توانید آن را در تلگرام/واتساپ یا کابل به گوشی منتقل و نصب کنید:
```
mobile_app/build/app/outputs/flutter-apk/app-release.apk
```

---

## 🔗 بخش چهارم: اتصال گوشی به سرور کامپیوتر

1. اطمینان حاصل کنید **کامپیوتر و گوشی هر دو به یک مودم Wi-Fi یکسان (یا هات‌اسپات)** متصل هستند.
2. فایل `start_server.bat` را روی کامپیوتر اجرا کنید تا داشبورد بالا بیاید.
3. اپلیکیشن را روی گوشی باز کنید.
4. روی دکمه **«اسکن کد QR از روی مانیتور»** ضربه بزنید و دوربین را جلوی کد QR نمایش داده شده در داشبورد کامپیوتر یا پنجره مشکی سرور بگیرید.
5. اتصال فوراً برقرار شده و تقویم و ویدیوهای شما همگام می‌شوند!

---

## 🌟 امکانات برجسته در اپلیکیشن موبایل:
- **اکسپورت ۱-کلیکه (1-Tap Export)**: با زدن یک دکمه در صفحه پست، ویدیو و کاور با کیفیت اصلی در پوشه عکس‌های گوشی شما ذخیره شده، متن کپشن در کلیپ‌بورد کپی می‌شود و دکمه ورود مستقیم به اینستاگرام نمایان می‌شود!
- **تقویم شمسی هوشمند**: دسته‌بندی خودکار پست‌ها به «امروز»، «فردا» و «روزهای آینده» با نام روزهای هفته (شنبه تا جمعه).
- **تغییر نما**: با زدن آیکون بالای صفحه، می‌توانید بین نمای فهرستی و نمای شبکه ۳×۳ شبیه پروفایل پیج اینستاگرام جابجا شوید.
- **تغییر وضعیت**: به سادگی تیک انتشار را روی گوشی بزنید تا بلافاصله در داشبورد کامپیوتر نیز تیک سبز «منتشر شده» درج شود.
